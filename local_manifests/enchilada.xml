<?xml version="1.0" encoding="UTF-8"?>
<!-- Draft manifest — must be validated against the AviumUI 16.2 base manifest before use on Crave. -->
<!--
  NOTE ON REMOTES & PROVENANCE:
  - AviumUI 16.2 default.xml defines:
      <remote name="github" fetch=".." review="review.lineageos.org" />
      <default revision="refs/heads/lineage-23.2" remote="github" />
  - AviumUI snippets/avium.xml defines:
      <remote name="avium" fetch="https://github.com/AviumUI" ... />

  TODO before Crave build execution:
  1. Confirm whether `github` (fetch="..") in AviumUI base manifest resolves `TheMuppets/*` cleanly.
     If not, declare an explicit `<remote name="themuppets" fetch="https://github.com/TheMuppets" />`.
  2. Confirm whether `LineageOS/android_hardware_oneplus` is already tracked in base manifests.
     If already present, remove it from this local manifest to avoid duplicate project errors during `repo sync`.
  3. Confirm the kernel revision tag (defaulting to refs/heads/lineage-23.2).
-->
<manifest>

  <!-- Optional Explicit Remotes (Uncomment if Crave base manifest does not resolve cross-org GitHub paths) -->
  <!--
  <remote name="github-lineage"    fetch="https://github.com/LineageOS" />
  <remote name="github-themuppets" fetch="https://github.com/TheMuppets" />
  -->

  <!-- Device Trees -->
  <project path="device/oneplus/enchilada"
           name="LineageOS/android_device_oneplus_enchilada"
           remote="github"
           revision="lineage-23.2" />

  <project path="device/oneplus/sdm845-common"
           name="LineageOS/android_device_oneplus_sdm845-common"
           remote="github"
           revision="lineage-23.2" />

  <!-- Kernel Source -->
  <project path="kernel/oneplus/sdm845"
           name="LineageOS/android_kernel_oneplus_sdm845"
           remote="github"
           revision="lineage-23.2" />

  <!-- Hardware HALs -->
  <!-- TODO: Verify if hardware/oneplus is already declared in AviumUI/LineageOS default manifest -->
    <!-- OnePlus hardware HALs -->
    <project
        name="LineageOS/android_hardware_oneplus"
        path="hardware/oneplus"
        remote="github"
        revision="lineage-23.2" />

    <!-- Dolby hardware support -->
    <project
        name="AviumUI-Devices/hardware_dolby"
        path="hardware/dolby"
        remote="github"
        revision="avium-16.2" />

  <!-- Proprietary Vendor Blobs -->
  <project path="vendor/oneplus/enchilada"
           name="TheMuppets/proprietary_vendor_oneplus_enchilada"
           remote="github"
           revision="lineage-23.2" />

  <project path="vendor/oneplus/sdm845-common"
           name="ColoMovo-Labs/proprietary_vendor_oneplus_sdm845-common"
           remote="github"
           revision="avium-16.2"
           clone-depth="1" />

</manifest>
